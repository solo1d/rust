# 闭包进阶

闭包就是加强版函数，也是第一类函数，这意味着它们可以添加变量，也可以作为参数传递给函数，甚至可以从函数返回。

**闭包和函数的区别：闭包能够识别其声明的环境，并且可以引用上下文环境中的任何变量。**

默认情况下，闭包将尝试以最灵活的方式捕获变量。只有当程序员需要以某种特定方式捕获值时，它们才会强制执行，实现程序员的意图。除非我们看到不同类型的闭包在运行，否则这并没有多大的意义。闭包在系统底层是匿名的结构，它实现了 3 个特征，用于表示闭包如何访问其上下文环境。

**闭包的3个特征：Fn闭包、FnMut闭包、FnOnce闭包。**

**闭包参数的类型是“首次调用时”由传入的参数决定的，而不是由变量名决定的。**

### Fn闭包

仅为读取访问变量的闭包实现 Fn 特征。它们访问的任何值都是引用类型（&T）。这是使用闭包的默认模式

```rust
fn main(){
    let a = String::from("Hey!");
    let fn_closure = || {  // ||是参数管道符， 中间存放参数,参数名与上下文无关
        println!("Closure says: {}", a); // 这里调用默认是引用，不会涉及所有权
    };
    fn_closure();
    println!("Main says: {}", a);
}
```

### FnMut闭包

当编译器检测出闭包改变了执行环境中引用的某个值时，它实现了 FnMut 特征。

```rust
fn main(){
    let mut a = String::from("Hey!");
    let mut fn_mut_closure = || {  // 这里添加了mut
        a.push_str("Alice"); // 影响了a的上下文
    };
    fn_mut_closure();
    println!("Main says: {:?}", a);
}
```

### FnOnce闭包

从执行环境中获取数据的所有权的闭包实现了 FnOnce 特征。该名称表示此闭包只能被调用一次。因此，相关的变量只能使用一次。这是构造和使用闭包最不推荐的方法，因为后续不能使用其引用的变量。

使用场景：标准库中用于生成新线程的 thread::spawn 方法

```rust
fn main(){
    let mut a = Box::new(23);
    let call_me = || {
        let c = a;
    };
    call_me(); // 只能调用这一次
    call_me(); // 这个会报错
}
```
