### 导入

可以在模块中以嵌套的方式导入（import）元素，这有助于减少导入操作的资源占用。

```rust
use std::sync::{Mutex, Arc, mpsc::channel};

fn main(){
    let (tx, rx) = channel();
}
```

### 再次导出

再次导出（re-export）允许我们有选择地从模块中暴露一些元素给外界。当我们使用Option 和 Result 类型时，就已经在享受再次导出的便利性了。如果构造模块时需要创建很多嵌套目录来存放大量的子模块，那么再次导出有助于减少必须输入的导入路径。采用了 pub use 关键字。

```rust
mod bar;
pub use bar::Bar;  // 重新导出 bar下的pub struct Bar结构体
```

### 隐私性

Rust 的隐私性**从模块（mod）层面开始**：默认所有项（函数、结构体、枚举、常量等）都是**私有的**，仅在当前模块内可见。

`pub` 可以让项对**模块外部**（包括其他 crate 和用户）公开。

`pub(crate)` 让项**仅在当前 crate 内可见**，对外部用户隐藏。

```rust
mod submodule;

// 这个函数对整个 crate 公开（即外部用户可见）
pub fn public_function() {
    println!("Public function called.");
}

// 这个函数仅在 crate 内部可见（pub(crate)）
pub(crate) fn crate_only_function() {
    println!("Crate-only function called.");
}

// 这个函数是私有的（默认），只在当前模块可见
fn private_function() {
    println!("Private function called.");
}

// 在根模块中，我们可以调用 crate_only_function 和 private_function
pub fn test_visibility() {
    crate_only_function(); // 合法
    private_function();    // 合法（同一模块内）
}

pub struct MyStruct {
    pub public_field: i32,   // 外部可读可写
    private_field: bool,     // 默认私有，外部不可访问
    pub(crate) pcr : bool,   // 这是当前 crate 内部公开的
}

// 我们也可以调用子模块中的东西
pub use submodule::submodule_public;
```

