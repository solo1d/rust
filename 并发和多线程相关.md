### 基础线程

```rust
use std::thread;

fn main() {
    // 使用 thread::spawn 会直接创建线程并执行，默认是线程分离
    let child: thread::JoinHandle<String> = thread::spawn(|| {
        println!("Thread!");
        String::from("Much") // 可以返回一个简单的结果
    });
    println!("Hello, world!");
    for i in 1..9999999{ // 被动等待子线程
        print!("")
    }
    println!("Hello, world!");
    println!("Hello, world!");

    // 手动等待子线程运行结束，并获取返回值
    let value: String = child.join().expect("Failed");
    println!("value： {}", value);
}
```

### 自定义线程

通过指定的API来配置线程的属性。

```rust
use std::thread::Builder;
fn main(){
    let my_thread = Builder::new()
            .name("Worker Thread".to_string()) // 配置线程名称 
            .stack_size(1024 * 4); // 配置堆栈大小
    let handle: Result<std::thread::JoinHandle<()>, std::io::Error> =
        my_thread.spawn(||{
            panic!("Oops!");
        });
    let child_status = handle.unwrap().join();
    println!("Child status: {:?}", child_status);
}
```

## 线程的并发模型

Rust 内置了两种流行的并发模型：通过**同步共享数据**和**通过消息传递共享数据**。

同步原语是允许多个线程以线程安全的方式访问或操作值的类型或语言构造。

##### 通过Arc类型共享所有权

```rust
use std::thread;
use std::sync::Arc;

fn main() {
    // 多线程Arc原子引用计数
    let nums: Arc<Vec<i32>> = Arc::new(vec![0, 1, 2, 3, 4]);
    let mut handles: Vec<thread::JoinHandle<()>> = vec![];

    for n in 0..5 {
        let nums: Arc<Vec<i32>> = nums.clone(); // 增加引用计数
        let handle: thread::JoinHandle<()> = thread::spawn(move || {
            println!("{}", nums[n]); // n 被 move 捕获，nums 是 Arc
        });
        handles.push(handle);
    }

    // 等待所有线程完成
    for handle in handles {
        handle.join().unwrap();
    }
    println!("nums:{:?}" , nums);
}
```

##### 修改线程中的共享数据

```rust
use std::thread;
use std::sync::{Arc, Mutex}; // 使用mutex互斥锁

fn main() {
    // 使用Mutex和arc来达到共享可变性
    let nums: Arc<Mutex<Vec<i32>>> = Arc::new(Mutex::new(vec![]));
    let mut handles: Vec<thread::JoinHandle<()>> = vec![];
    for n in 0..5 {
        let ns: Arc<Mutex<Vec<i32>>> = nums.clone();
        let handle: thread::JoinHandle<()> = thread::spawn(move || {
            let mut vec: std::sync::MutexGuard<'_, Vec<i32>> = ns.lock().unwrap();
            vec.push(n);
        });
        handles.push(handle);
    }
    for handle in handles {
        handle.join().unwrap();
    }
     // 最终打印结果（顺序可能乱，但包含 0~4）
    let vec: std::sync::MutexGuard<'_, Vec<i32>> = nums.lock().unwrap();
    println!("{:?}", *vec);
}
```

## 锁和同步相关

**互斥锁 Mutex 类型不提供共享可变性。可以和Arc组合来实现共享可变性。**

**读写锁 RwLock 类型提供共享可变性。但可能会遇到写入者饥饿问题。**

```rust
use std::thread;
use std::sync::RwLock;

fn main() {
    let m = RwLock::new(5);
    let c = thread::spawn(move || {
        {
            *m.write().unwrap() += 1;  // 写锁
        }
        let updated = *m.read().unwrap(); // 读锁
        updated
    });
    let updated = c.join().unwrap();
    println!("{:?}", updated);
}
```

**通过消息传递来进行通信，这种通信模型避免了需要用户显示锁定的要求。**

- `std::sync::mpsc` 提供了一个无锁定的**多生产者**，**单订阅者**（消费者）队列。(MPSC)
- 通道可用于将数据从一个线程发送到另一个线程。
  - channel 异步的无限缓冲通道, send 方法永远不会阻塞
  - sync_channel 同步的有界限缓冲通道,当它被填满时，send 方法会被阻塞，直到通道中出现更多空间，但 recv 永远不会阻塞

```rust
//  channel 异步的无限缓冲通道
use std::thread;
use std::sync::mpsc::channel;

fn main(){
    let (tx, rx) = channel();
    let join_handle = thread::spawn(move || {
        while let Ok(n) = rx.recv() { // 循环接收
            println!("Received {}", n);
        }
    });
    let tx2 = tx.clone();
    let join_handle_send2 = thread::spawn(move|| {
        for i in 200..300{
            tx2.send(i).unwrap();    // 发送
        }
    });
    for i in 0..100{
        tx.send(i).unwrap();    // 发送
    }
    drop(tx); // 释放发送端
    join_handle.join().unwrap();
    join_handle_send2.join().unwrap();
}
```

```rust
// sync_channel 同步的有界限缓冲通道
use std::thread;
use std::sync::mpsc;

fn main(){
    let (tx, rx) = mpsc::sync_channel(1); // 缓冲区就给1，多个写入会阻塞
    let tx_clone = tx.clone();
    let _ = tx.send(0);

    thread::spawn(move || {
        let _ = tx.send(1); //阻塞，等待读取
    });

    thread::spawn(move || {
        let _ = tx_clone.send(2); // 阻塞，等待读取
    });
    println!("Recived {} via th channel", rx.recv().unwrap());
    println!("Recived {} via th channel", rx.recv().unwrap());
    println!("Recived {} via th channel", rx.recv().unwrap());
    // 写入的内容已经全部读取完毕了
    let s = rx.recv();  // 这里不会阻塞
    println!("s {:?}",s); // s Err(RecvError)
}
```



## 线程安全

线程安全是一种类型或者一段代码的属性，当由多个线程执行或访问时，不会导致意外行为。它指的是这样一种思想，即数据对于读取是一致的，多个线程同时写入时，数据不会被损坏。

Rust 仅保护用户不会受到数据竞争问题的困扰。大多数线程安全位都已经嵌入 spawn 函数的类型签名中。

```rust
// 函数签名
pub fn spawn<F, T>(f: F) -> JoinHandle<T>
where
    F: FnOnce() -> T,	// 表示 F 实现了一个只能被调用一次的闭包,通过值获取所有内容并移动从环境中引用的项
    F: Send + 'static, // 这表示闭包必须是发送型,且必须具有'static 的生命周期，同时执行环境中闭包内引用的任何类型必须是发送型，必须在程序的整个生命周期内存活。
    T: Send + 'static, // 来自闭包的返回类型 T 必须实现 Send+'static 特征。
{  ...  }
/*
Send 是一种标记性特征。它只用于类型级标记，意味着可以安全地跨线程发送值；并且大多数类型都是发送型。
未实现 Send 特征的类型是指针、引用等。
Send 是自动型特征或自动派生的特征。复合型数据类型，例如结构体，如果其中的所有字段都是 Send 型，那么该结构体实现了 Send 特征。
*/
```

##### send

Send 类型可以安全地发送到多个线程，这表明该类型是一种移动类型。非 Send 类型的是指针类型，例如&T，除非 T 是 Sync 类型。

```rust
pub unsafe auto trait Send { }
// 上述定义中需要特别留意的有 3 点：首先，它是不包含任何函数体或元素的标记特征；其次，它以 auto 关键字作为前缀，因为它适用于大多数类型的隐式实现；最后，它是一个不安全的特征，因为 Rust 希望开发者能够明确地选择，并确保它们的类型具有内置的同步线程安全。
```

##### sync

```rust
pub unsafe auto trait Sync { }
// 这表示实现此特征的类型可以安全地在线程之间共享。如果某些类型是 Sync 类型，那么指向它的引用，换句话说相关的&T 是 Send 类型。这意味着我们可以将对它的引用传递给多线程。
```



