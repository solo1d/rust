


## 类型推断
类型推断对静态类型语言来说非常有用，因为这使代码更易于编写、维护及重构。
在底层，被称为类型检查器的编译器组件使用 Hindley Milner 类型推断算法来确定具体的局部变量的类型。它是一组根据其用法建立表达式类型的规则。实际上，它可以根据环境和类型的使用方式来推断类型。

## 类型别名
类型别名能够将复杂类型签名的类型声明为一个简单的名称。
在编译期间，别名会被替换或者拓展为原始类型。
原始类型带泛型，那么别名也得带，包含生命周期也是如此。
```rust
// 原始声明
pub struct Par<T>{ inner : T };
pub struct Per<E>{ inner : E };
pub fn parse<T,E>() -> Result<Par<T>,Per<E>>{ ; }

// 通过类型别名简化后  type
type PaerRes<T,E> = Result<Par<T>,Per<E>>;
pub fn parse<T,E>() -> PaerRes<T,E>{ ; }

// 带生命周期的别名
struct SuperCmp<'s>{ stream: &'a [u8] };
type Parser<'s> = SuperCmp<'s>;
```



