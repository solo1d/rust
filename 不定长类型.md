**编译器在编译时不知道它占用多少字节内存，所以它不能直接站在栈上，必须永远躲在指针（引用或 Box）的背后。**

| 不定长类型                  | 具体含义                                                    | 为什么不定长？                                         |
| :-------------------------- | :---------------------------------------------------------- | :----------------------------------------------------- |
| **`[T]`**（切片）           | 动态大小的数组（注意，不带数字的 `[u8]`，而不是 `[u8; 3]`） | 你不知道数组里到底有几个元素。                         |
| **`str`**（字符串切片）     | 字符串数据（注意，是 `str`，不是 `&str`）                   | UTF-8 字符长度不一，无法提前知道字节数。               |
| **`dyn Trait`**（特征对象） | 比如 `dyn Iterator` 或 `dyn Fn`                             | 编译器不知道具体是哪个结构体实现了该特征，大小不固定。 |

在泛型中，**所有泛型参数默认都隐式带上了 `Sized` 约束**，意思是：“这个类型必须是定长的”。

```rust
// 你写的：
fn foo<T>(x: T) { ... }
// 编译器实际理解为：
fn foo<T: Sized>(x: T) { ... }
```

如果你想允许传入**不定长类型**（比如 `str`、`[u8]` 或 `dyn Trait`），你必须手动去掉这个默认约束，写成 **`T: ?Sized`**（读作：“可能不定长”）。

```rust
// 允许传入 &str（定长指针）或 &[u8] 等胖指针
fn foo<T: ?Sized>(x: &T) { ... }
```

```rust
// 错误示范：试图直接返回不定长类型 [bool]
fn compute_primes_bad(limit: usize) -> [bool] { // 编译报错！
    let sieve = vec![true; limit];
    sieve // 错误：无法在编译时知道大小
}

// 你必须把它放在指针后面，变成了胖指针：
// 正确做法：返回的 &[bool] 实际上是一个胖指针（地址+长度）
fn compute_primes_bad(limit: usize) -> &'static [bool] {
    // ... 但这需要静态生命周期，仅做演示
}
```

默认情况下，rust将str的引用类型创建为`'static str`。

不定长类型的实例。

- [T]：这是类型 T 切片，它们只能用作&[T]或者&mut [T]。
- dyn Trait：这是一个特征对象，它们只能用作&dyn Trait 或者&mut dyn Trait 类型。
- 任何将不定长类型作为其最后一个字段的结构体也被视为不定长类型。
- str 其内部只有一个[u8]，但是能够保证其中的字节都是有效的 UTF-8 格式。