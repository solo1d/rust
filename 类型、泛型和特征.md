## 泛型函数

泛型函数就是一个种提供多态代码错觉的简易写法，可以通过 nm 命令去获取符号函数，可以找达到两个 give_me 函数。

```rust
// main.rs
fn give_me<T>(value: T){
    let _ = value;
}

fn main(){
    let a = "generics";
    let b = 1024;
    give_me(a);
    give_me(b);   
}
```

## 泛型

```rust
// 泛型元组结构体
struct GenericStruct<T>(T);
// 泛型普通结构体
struct Container<T>{
    item: T
}
// 泛型枚举
enum Transmission<T>{
    Signal(T),	// 泛型值
    NoSignal    // 变体(无值变体)
}
// 泛型实现
impl<T> Container<T>{
    fn new(item:T) ->Self{
        Container { item }
    }
}
// 针对泛型的 u32 作为具体实现的专门化泛型
impl Container<u32>{
    fn sum(item:u32)->Self{
        Container { item }
    }
}

fn main(){
    let c1 = Container::new(42);          // 类型被推导为 Container<i32>
    let c2 = Container::new("hello");     // Container<&str>
    let c3 = Container::new(3.14);        // Container<f64>
    // 也可以显式指定类型
    let c4: Container<bool> = Container::new(true);
    let c5 = Container::sum(100);          // 类型为 Container<u32>
    // 或者显式注解
    let c6: Container<u32> = Container::sum(200);
    
    let mut v2 = Vec::new();
    v2.push(2);	// 通过这个方法去推断上面的类型
    
    // turbofish 运算符，指代 ::<> 这种写法
    // 可以出现在函数名之后和圆括号之前
    let v3 = Vec::<u8>::new();  
    let num_for_str = str::parse::<u8>("34").unwrap();
    // 上面只有实现了 FromStr的接口或者特征的类型才能这样传递
    let b = identity::<f64>(3.14);  // 可以显式写，但不是必须
}
```



## 特征

特征是一个元素，它定义了一组类型可以选择性实现的契约或共享行为。类似于接口。

特征可以在不同的类型之间建立关联，包括存在在 闭包、运算符、智能指针、循环、基础类型等。

当特征与泛型搭配使用的时候，可以限制传递到API的参数范围。

特征也可以是泛型的。

特征是可以被继承的，但他的实现类似于组合，都得实现里面的接口。

```rust

struct Audio(String);
struct Video(String);


pub trait Playable {  // 创建一个特征，并包含两个方法，默认是私有的
    fn play(&self);  // 实例方法
    fn pause(){  // 关联方法
        println!("Paused");
    }
}

//实现特征的实例方法
impl Playable for Audio {
    //内部是特征实现
    fn play(&self) {
        println!("Now playing:{}",self.0);
    }
}
impl Playable for Video {
    fn play(&self) {
        println!("Now playing: {}",self.0);
    }
}

fn main() {
    println!("Super player!");
    let audio = Audio("ambient_music.mp3".to_string());
    let video = Video("big_buck_bunny.mkv".to_string());
    audio.play();  // 调用
    video.play();
    /*
     * 输出： 
     * Super player!
     * Now playing:ambient_music.mp3
     * Now playing: big_buck_bunny.mkv
     */
}
```

特征继承和基础类型的特征

```rust
trait Vehicle{
    fn get_price(&self) -> u64;
}

// 继承特征
trait Car:Vehicle {
    fn module(&self) -> String;
}

struct TeslaRoadster{
    model : String,
    release_date : u64
}

impl TeslaRoadster {
    fn new(model: &str, release_date:u64)->Self {
        Self {model: model.to_string(), release_date}
    }
}
// 既要实现Car的特征，也要实现 Vehicel父特征的内容
impl Car for TeslaRoadster {
    fn module(&self) -> String {
        "Tesla Roadster I ".to_string()
    }
}

impl Vehicle for TeslaRoadster {
  fn get_price(&self) -> u64{
    return 1
  }
}

// 基础类型的特征
impl Vehicle for  u64 {
    fn get_price(&self) -> u64{
        return 1
    }
}

fn main() {
    let my_roadster = TeslaRoadster::new("Tesla Roadster II", 22);
    println!("{} is priced at ${}", my_roadster.model, my_roadster.get_price());
    let uu:u64  = 0;
    println!("u64 {}", uu.get_price());
}
```

泛型特征和关联特征类型，比较适合Iterator迭代器

```rust
// 泛型特征声明
trait FromS<T> {
    fn froms(t : T) -> Self;
}
// 另一种 关联类型特征, 比泛型更好的选择
trait Foo {
    type  TT;
    fn get_value(self) -> Self::TT;
}

struct sts {
}

// 泛型特征的某个实现
impl FromS<sts> for sts {
    fn froms(t : sts ) -> Self{
        return sts{}
    }
}
// 关联类型特征
impl Foo for sts{
    type  TT = sts;  // 这里指定了类型
    fn get_value(self) -> Self::TT {
        return sts{}
    }
}

fn main(){
    let f: sts = sts{}; // 正常创建
    let f2 = sts::froms(sts{}); // 使用范性特征
    let f3 = f2.get_value(); // 使用关联性特征
}
```

特征区间

```rust
struct Game;
struct Enemy;
struct Hero;

trait Loadable {
    fn init(&self);
}

impl Loadable for Enemy {
    fn init(&self){
        println!("Enemy init")
    }
}

impl Loadable for Hero {
    fn init(&self){
        println!("Hero init")
    }
}

impl Game {
    // 这里限定了传入的参数，也就是范围，必须是实现了 Loadable 的特征类型
    fn load<T:Loadable>(&self, entity: T){
        entity.init();
    }
}

// Add 特征区间， 具有相加特征，是支持泛型的
use std::ops::Add;
fn add_thing<T:Add>(fst: T, snd:T){
    let _= fst + snd;
}

fn main(){
    let game = Game;
    game.load(Enemy);
    game.load(Hero);
}
```

where 语句

```rust
/*
pub fn parse<F>(&self) -> Result<F, <F as FromStr>::Err>  
where F: FromStr {...} 
*/
use std::fmt::Display;

// where同等结构， 对类型进行特征区间限制
struct Foo<T:Display> {
    bar : T
} 

// 或者下面的写法，也是对类型进行特征区间限制
struct Bar<F> where F: Display {
    inner : F
}

fn main(){
}
```

```rust
use std::fmt::Debug;

trait Eatable {
    fn eat (&self);
}

#[derive(Debug)]
struct Food<T>(T);

#[derive(Debug)]
struct Apple;

// 泛型的特征，将泛型传入Food， 并限定泛型的类型必须是Debug
impl<T> Eatable for Food<T> where T: Debug {
    fn eat(&self){
        println!("Eating {:?}", self); // 必须是debug类型才能使用 {:?} 输出
    }
}
fn eat<T>(val: T) where T:Eatable {
    val.eat();
}

fn main(){
    let apple = Food(Apple);
    eat(apple);
}
```

使用符号“+”为泛型指定多个特征区间

```rust
trait Eat {
    fn eat(&self){
        println!("eat");
    }
}

trait Code {
    fn code(&self){
        println!("code");
    }
}
trait Sleep {
    fn sleep(&self){
        println!("sleep")
    }
}

// 组合特征区间
trait Programmer: Eat + Code + Sleep {
    fn animmer(&self){
        self.eat();
        self.code();
        self.sleep();
        println!("repeat!");
    }
}

// 实现所有的特证区间
struct Bob;
impl Programmer for Bob{}
impl Eat for Bob{}
impl Code for Bob{}
impl Sleep for Bob{}

fn main(){
    Bob.animmer();
}
```

特征区间与impl特征语法

```rust
use std::fmt::Display;
//  impl Display 这个是特征语法
fn show_me(val: impl Display){
    println!("{}", val);
}

// 接受两个数字，并返回这两个数字相加的闭包
// 闭包的底层结构由实现了一系列特征的结构体组成（Fn(T)->U） 特征就是其中之一
fn lazy_adder(a:u32, b:u32) -> impl Fn()->u32 { // 他返回一个闭包,只有调用闭包才会进行运算
    move || a + b
}

//在参数位置使用它,就不能使用 turbofish 运算符来进行使用
fn surround_with_braces(val : impl Display) -> impl Display {
    format!("{{{}}}", val)
}

fn main(){
    show_me("Trait bounds are awesom"); // 输出Trait bounds are awesom
    let add_later = lazy_adder(1024, 2048);
    println!("{:?}", add_later()); // 输出 3072
    println!("{}", surround_with_braces("Hello"));  // 输出 {Hello}
}
```

### 标准库特征和使用

```rust
use std::ops::Add;
use std::convert::From;
use std::fmt::{Formatter, Display ,Result};

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

// 属性 default 会自动实现初始化值为0，泛型成员得实现这个属性（特征）
// 添加了内置特征进行自动派生
#[derive(Default,Debug,PartialEq, Clone, Copy)]
struct Complex<T>{
    re: T,  // 实部
    im : T  // 虚部
}

impl<T> Complex<T> {
    fn new(re:T, im:T ) ->Self {
        Complex { re, im }
    }
}

// 具有RHS泛型的特征，RHS默认是Self 是实现此特征的类型别名，
// pub trait Add<RHS = Self>{
//     type Output;
//     fn add(self, rhs:RHS) -> Self::Output;
// } 
impl<T:Add<T,Output = T>> Add for Complex<T>{
    type Output = Complex<T>;
    fn add(self, rhs: Complex<T>) ->Self::Output{
        Complex { re:self.re + rhs.re, im:self.im + rhs.im}
    }
}
impl<T> From<(T,T)> for Complex<T>{
    fn from(value: (T,T) ) -> Complex<T>{
        Complex { re: value.0, im:value.1 }
    }
}
// 实现 Display特性
impl<T:Display> Display for Complex<T>{
    fn fmt(&self, f: &mut Formatter) -> Result {
        write!(f, "{} + {}i", self.re, self.im)
    }
}

// 测试命令  cargo test --  --no-capture
#[cfg(test)]
mod tests {
    use core::convert::From;
    use super::Complex;

    #[test]
    fn complex_basics(){
        let first = Complex::new(3,5);
        let second : Complex<i32> = Complex::default(); // 这里调用了default特征
        assert_eq!(first.re, 3);
        assert_eq!(first.im, 5);
        assert!(second.re == second.im);
    }

    #[test]
    fn complex_addition(){
        let a = Complex::new(1,-2);
        let b = Complex::default();
        let res = a + b;
        assert_eq!(res, a );
    }
    #[test]
    fn complex_from(){
        let a= (2345,456);
        let complex = Complex::from(a);
        assert_eq!(complex.re, 2345);
        assert_eq!(complex.im, 456);
    }
    #[test]
    fn complex_display(){
        let my_imaginary = Complex::new(2345, 456);
        println!("{}", my_imaginary);
    }
}
```



## 特征对象实现多态

```rust
use std::fmt::Debug;
use std::fmt::Display;

#[derive(Debug)]
struct Square(f32);
#[derive(Debug)]
struct Recuangle(f32,f32);

trait Area:Debug {
    fn get_area(&self)->f32;
}

impl Area for Square {
    fn get_area(&self)->f32 {
        self.0 * self.0
    }
}
impl Area for Recuangle {
    fn get_area(&self)->f32 {
        self.0 * self.1
    }
}
// 将 dyn trait 作为参数
fn show_me(item: &dyn Display) {
    println!("{}", item);
}

fn main(){
    // 存储实现了特征类型的对象指针
    let shapes: Vec<&dyn Area> = vec![&Square(3f32), &Recuangle(4f32, 2f32)];
    for s in shapes {
        // 基本多态
        println!("{:?}  {}",s, s.get_area());
    }

    show_me(&"Hello trait object");
}
```
