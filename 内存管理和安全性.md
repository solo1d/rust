**Rust 在堆上分配内存的唯一方法是通过智能指针类型**

Rust 中的智能指针类型实现了 Drop 特征，它指定了如何释放值所使用的内存，并且在语义上定义了类似于 C++中析构函数的方法。除非用户编写自定义的智能指针类型，否则你永远不需要在其类型上实现 Drop 特征。

## 内存安全三原则

包括所有权、借用、生命周期

### 所有权

```rust
#[derive(Debug, Clone)] // Clone
struct Dumy {
    items: u32
}

fn main() {
    let a = Dumy{ items : 54};
    let b = a.clone();
    println!("a: {:?}, b: {:?} ", a, b);
}
```

移动语义

```rust
fn take_the_n(_n:u8) {}
fn take_the_s(_s: String){}
fn take_the_s2(_s: String) ->String{
    println!("inside function {}", _s);
    _s
}

fn main(){
    let n = 5;
    let s = String::from("string");
    take_the_n(n);  // 由于基元类型实现了Copy特征，这里就会自动执行该特征
    take_the_s(s.clone()); // 这里如果不调用clone 会出现移动语义，编译报错

    println!("n is {}", n);
    println!("s is {}", s);
    // 另一种写法,将数据传入，然后函数进行了返回，也是移动语义
    let s = take_the_s2(s);
    println!("s is {}", s);
}
```

match默认会移动语义

```rust
#[derive(Debug)]
enum Food{
    Cake,
    Pizza,
    Salad
}

#[derive(Debug)]
struct Bag{
    food:Food
}
fn main(){
    let bag = Bag{ food: Food::Cake};
    match bag.food { // match 移动类型默认也会被移动，后续会失效
        Food::Cake => println!("i got cake"),
         a => println!("i got {:?}", a)
    }
    println!("{:?}", bag);
}
```

```rust
struct  Item(u32);
impl Item {
    fn new() ->Self {
        Item(1024)
    }
    fn take_item(self){
        // 调用这里也会产生移动语义，结束后self会被释放
    }
}

fn main(){
    let it = Item::new();
    it.take_item();  // 移动了，后续就无法使用 it 了
    println!("{}", it.0); // 这里无法编译
}
```

闭包中的所有权

```rust
#[derive(Debug)]
struct Foo;

fn main(){
    let a= Foo;
    let closure = || {
        let b = a;  // a的所有权移机移动给了b 
    };
    println!("{:?}", a); // 这里会报错
}
```

### 借用

```rust
#[derive(Debug)]
struct Foo(u32);

fn main(){
    let foo = Foo(1); 
    let bar = &foo;  // 在变量前面加 & 表示不可变借用
    println!("Foo is {:?}", foo);
    println!("Bar is {:?}", bar);
}
```

```rust
// 可变参数借用
fn take_the_n(n: &mut u8){
    *n += 2
}
fn take_the_s(s :&mut String){
    s.push_str("ing");
}

fn main(){
    let mut n = 5;
    let mut s = String::from("Borrow");
    take_the_n(&mut n);
    take_the_s(&mut s); 

    println!("n changed to {}", n); // 7 
    println!("s changed to {}", s); // Borrowing
}
```

```rust
#[derive(Debug)]
enum Food{
    Cake,
    Pizza,
    Salad
}

#[derive(Debug)]
struct Bag{
    food:Food
}
fn main(){
    let bag = Bag{ food: Food::Cake};
    match bag.food { // match 移动类型默认也会被移动，后续会失效
        Food::Cake => println!("i got cake"),
        ref a => println!("i got {:?}", a) // ref 可以通过引用来匹配元素
    }
    println!("{:?}", bag);
}
```

关于 self相关实例的借用规则

- `&self` ， 对成员具有不可变的访问权限
- `&mut self` ，能够可变的借用self实例
- `self` ，拥有他的实例所有权，并在类型后续调用时失效，离开当前作用域时，该值生命周期结束

### 生命周期

Rust 中的所有引用都附加了生命周期信息。生命周期定义了引用相对值的原始所有者的生存周期，以及引用作用域的范围。

Rust 中的生命周期能够确保引用的存续时间不超过它指向的值。

对于编译器无法通过分析代码来确定值的生命周期的情况，我们需要通过在代码中添加一些注释来帮助编译器达到上述目的。为了与标识符区分，生命周期注释带有 ' '前缀。

```rust
// 添加了生命周期注释 a标识符
struct SomeRef<'a, T> {
    part: &'a T
}

fn main(){
    let a = SomeRef { part: &43};
    println!("{}", a.part);
}
```

```rust
// 通过将生命周期用作泛型参数来解决无效引用。
fn main(){
    // 关键字 static 修饰的生命周期意味着这些引用在程序运行期间都是有效的
    // 所有文本字符都具有'static 的生命周期，并且它们会被转到已编译对象代码的数据片段中
    let _a: &'static str = "I live forever";
    println!("{}", _a);
}
```

只要在函数或类型定义中存在引用，就会涉及生命周期

- 输入型生命周期：函数参数上的生命周期注释当作引用时被称为输入型生命周期。
- 输出型生命周期：函数返回值上的生命周期参数当作引用时被称为输出型生命周期。

**任何输出型生命周期都源自输入型生命周期，我们不能拥有独立于输入型生命周期的输出型生命周期。它只能是一个小于或等于输出型生命周期的生命周期。**

以下是省略生命周期时需要遵守的一些规则。

- 如果输入型生命周期仅包含单个引用，那么假定输出型生命周期也仅包含单个引用。
- 对于涉及 `self` 和`&mut self` 的方法，输入型生命周期是针对`&self`进行推断的。

需要手动指定生命周期的地方有：

1. 函数签名
2. 结构体和结构体字段 （引用类型字段）
3. `impl` 代码块

```rust
// 下面的<'a> 声明一个生命周期参数,
//    它告诉编译器：Number 结构体里包含的引用，其有效时间由这个 'a 描述。
#[derive(Debug)]
struct Number<'a> {
    num: &'a u8  // 这个 u8 的引用，它的有效时间至少和 'a 一样长。
}
// 上面组合起来就表示： num 指向的数据，存活时间 不能短于 Number 本身的存活时间。

// 这个是impl的生命周期，大多数的情况下能够从类型自身进行推断的
//  也可以使用 <'_> 来省略签名
impl<'a> Number<'a> {
    fn get_num(&self) -> &'a u8 {
        self.num
    }
    fn set_num(&mut self, new_number: &'a u8){
        self.num = new_number
    }
}

fn main(){
    let a = 10 ;
    let mut num = Number {num : &a};
    println!("{:?}", num.get_num());
    num.set_num(&23);
}
```

```rust
// 多生命周期
// schema 和reader 都分别具有不同的生命周期
struct Decoder<'a, 'b, S, R >{
    schema: &'a S,
    reader: &'b R
}

// 生命周期子类型
// 可以在 impl 代码块中声明生命周期之间的关系
impl<'a, 'b, S, R> Decoder<'a, 'b, S, R>
where 'a : 'b {
    // 使用 where 语句在 impl 代码块中指定了关系：'a:'b。这表示'a 的生命周期比'b 长，或者相等

}

fn main(){
    let a :Vec<u8> = vec![];
    let b :Vec<u8> = vec![];
    let decoder = Decoder {schema: &a, reader: &b};

}
```

## 指针类型

三种指针类型：引用（安全的指针）、原始指针、智能指针

### 原始指针

```rust
fn main() {
    let a = &65;
    let a_raw_ptr = a as *const u32;
    let b = &mut 65;
    let a_raw_ptr = b as *mut u32;
}
```

### 智能指针

智能指针具有与之相关联的额外元数据和代码，会在创建和销毁指针时被调用。
智能指针超出作用域时能够自动释放底层资源是采用它们的主要原因之一。
智能指针的两大特性： Drop 和 Deref 

**Drop是特征，会自动释放相关值超出作用域后占用的资源，类似于析构函数。**
Drop包含一个 drop 方法，当对象超出作用域时就会被调用，该方法以 `&mut self` 作为参数。
使用 drop 释放值是以LIFO的方式进行的。

```rust
struct Character {
    name: String,
}

impl Drop for Character {
    fn drop(&mut self) {
        println!("{} went away", self.name);
    }
}

fn main() {
    let steve = Character { name : "Steve".into() };
}
```

**智能指针通常会实现Deref和DerefMut特征，他允许用户对这些类型能够使用解引用`*`运算符，其中 Deref 提供只读签名， DerefMut提供对地产类型的可变引用类型签名。**

- 智能指针的种类：
  - `Box<T>` 它提供了最简单的堆资源分配方式。Box 类型拥有其中的值，并且可用于保存结构体中的值，或者从函数返回它们。<mark>如果基础类型存在Copy特征，那么Box实例会成为副本，否则会发生移动</mark>。
  - `Rc<T>` 他用于引用计数，每当获取新引用时，计数器会执行递增操作，并在用户释放引用时对计数器执行递减操作，当计数器为0时，该值将被移除。它内部存在两种引用， 强引用`Rc<T>` 和 弱引用`Weak<T>`，只有强引用为0才会释放。
  - `Arc<T>` 原子引用计数，和 Rc<T>差不多，但他具有原子性，能保证多线程的安全性。
  - `Cell<T>` 提供了 Copy 特征的类型的内部可变性，可以获得多个可变引用。但是他不支持多线程。
  - `RefCell<T>` 提供了类型的内部可变性，并且不需要实现Copy特征，它用于运行时的锁定以确保安全性，但是他不支持多线程。

```rust
// Box<T> 智能指针的基础使用
fn box_ref<T>(b:T) -> Box<T>{
    let a = b;
    Box::new(a)
}

struct Foo;
struct Node {
    data: u32,
    next: Option<Box<Node>>  // 单链表，这里表示他是个指针+枚举标记
}

fn main(){
    let boxed_one: Box<Foo> = Box::new(Foo);
    let unboxed_one: Foo = *boxed_one;
    box_ref(unboxed_one);

    // 这里创建一个节点
    let node3 = Box::new(Node { data: 3, next: None });
    // 这里创建一个节点，将node3 移动进去， 这个时候 node3 就失效了
    let node2 = Box::new(Node { data: 2, next: Some(node3) });
    let nodeRoot = Box::new(Node { data: 1, next: Some(node2) });
    // 遍历
    let mut current = Some(&nodeRoot); // 这里要取引用
    while let Some(cur) = current {
        println!("Data: {}", cur.data);
        // 我们需要取其引用，用 as_ref() 将 Option<Box> 转为 Option<&Box>
        current = cur.next.as_ref();
    }
}
```

```rust
// Rc<T> 引用指针的使用
use std::{rc::Rc , rc::Weak, cell::RefCell};

#[derive(Debug)]
struct LinkedList<T>{
    head: Option<Rc<LinkedListNode<T>>>
}

#[derive(Debug)]
struct LinkedListNode<T>{
    next: Option<Rc<LinkedListNode<T>>>,
    prev: RefCell<Option<Weak<LinkedListNode<T>>>>,
    data: T
}

impl<T> LinkedList<T>{
    fn new() -> Self {
        LinkedList {head: None }
    }
    fn append(&mut self, data: T) ->Self {
        let new_node: Rc<LinkedListNode<T>> = Rc::new(LinkedListNode{
            data: data,
            next: self.head.clone(), // Rc的 .clone() 是增加引用计数，不是拷贝
            prev: RefCell::new(None)
        });
        match self.head.clone() {
            Some(node) =>{
                let mut prev: std::cell::RefMut<'_, Option<Weak<LinkedListNode<T>>>> = node.prev.borrow_mut();
                *prev = Some(Rc::downgrade(&new_node));
            },
            None =>{}
        }
        LinkedList {
            head: Some(new_node)
        }
    }
}

fn main(){
  let list_of_nums: LinkedList<i32> = LinkedList::new().append(1).append(2).append(3);
  println!("nums: {:?}", list_of_nums);
}
```

```rust
// Cell<T>
use std::cell::Cell;

#[derive(Debug)]
struct Bag {
    item: Box<u32>
}

fn main(){
    let bag = Cell::new(Bag{item: Box::new(1)});

    let hand1 = &bag;
    let hand2 = &bag;
    hand1.set(Bag{item:Box::new(2)});
    hand2.set(Bag{item:Box::new(3)});
}
```

```rust
// RefCell<T>
use std::cell::RefCell;

#[derive(Debug)]
struct Bag {
    item: Box<u32>
}

fn main(){
    let bag = RefCell::new(Bag{item: Box::new(1)});

    let hand1 = &bag;
    let hand2 = &bag;
    *hand1.borrow_mut() = Bag{item:Box::new(2)}; // 新的可变借用
    *hand2.borrow_mut() = Bag{item:Box::new(3)}; // 新的可变借用
    let borrowed = hand1.borrow(); // 接收一个新的不可变借用
    println!("{:?}", borrowed);
}

```

```rust

```
