##### HashMap操作

```rust
use std::collections::HashMap;

fn main(){
    let mut map = HashMap::new();
    map.insert("one", 1);
    map.insert("two", 2);
    // 获取和修改map内的元素数值
    if let Some(v) = map.get_mut("one") {
        *v += 1;
        println!("inc {}",v);
    }else{
        println!("None");
    }
    // 另一种 entry API方法
    map.entry("one").and_modify(|e| *e += 1);

    // 获取值的判断，当one不存在的话，就默认给个0
    let increment = match map.get("one") {
        Some(val) => val + 1,
        None => 0
    };
    // 另一种方法
    let inc2 = if let Some(v) = map.get("one") {
        v + 1
    }else {
        0
    };
    // unwrapping 方法，不是很安全，仅仅当确定某个值一定存在的时候才能使用，否则会发生异常
    // 他会在option上调用解压缩方法nwrap()和expect()方法
    let inc_v = map.get("two").unwrap() + 1;
    println!("{}",inc_v);
    println!("{:?}", map);   
}
```



#### Result

Result 和 Option 类似，但具有一些额外的优点，即能够存储和错误上下文有关的任意异常值，而不只是 None。当我们希望知道操作失败的原因时，此类型是合适的


