在 Rust 中，循环（loop）也是一个表达式，当我们中止（break）它时默认会返回()。这意味着循环也可以用于为具有 break 语句的变量赋值
```rust
fn main(){
    let mut i =0;
    // 循环
    let counter = loop {
        i += 1 ;
        if i == 10 {
            break i; // 这里break 返回值 并送给 counter
        }
    };
    println!("{}",counter);
}
```

```rust
let v = vec![10, 20, 30];
let mut i = 0;
while i < v.len() {
    println!("下标 {}: 值 {}", i, v[i]);
    i += 1;
}
```

```rust
let v = vec![10, 20, 30];

// 最标准写法：拿到下标和值
for (index, value) in v.iter().enumerate() {
    println!("下标 {}: 值 {}", index, value);
}

// 如果你只想看下标，不关心值
for index in 0..v.len() {
    println!("下标: {}", index);
}

// 如果你需要修改元素，可以写：
for item in &mut v {
    *item += 1;
}
```

