# 异常处理
但是在 Rust 中，返回值表示适当的异常类型，并且支持用户自定义类型。
Rust 中的大多数错误处理都是通过 Option 和 Result 这两种通用类型完成的。它们充当包装器类型，建议那些可能失败的 API 通过将它们放在这些类型中来返回实际值。这些类型使用枚举和泛型的组合进行构建。
枚举可以用于存储程序成功执行与否的状态，而泛型允许它们在编译时具体化，以便它们在任意状态下存储值。
这些类型还带有许多简便的方法（通常被称为组合器），允许你轻松地使用、组合或转换内部值。有一点需要注意：Option 和 Result 类型是标准库中的普通类型，都是包装器类型，这意味着它们不是编译器内置类型，所以编译器会将它们和其他普通函数一视同仁。任何人都可以使用枚举和泛型功能创建类似的错误抽象类型。

#### option处理空值
```rust
use std::collections::HashMap;

fn main(){
    let mut map = HashMap::new();
    map.insert("one", 1);
    map.insert("two", 2);
    // 这里可以看到 通过获取到的 option<i32> 来进行判断，避免空值
    let increment = match map.get("one") {
        Some(val) => val + 1,
        None => 0
    };

    println!("{}", increment);
    // 这里也是同样的
    let inc2 = if let Some(v) = map.get("one") {
        v + 1
    }else {
        0
    };
    println!("{}",inc2);   
    
    // unwrapping 方法，不是很安全，仅仅当确定某个值一定存在的时候才能使用，否则会发生异常
    let inc_v = map.get("two").unwrap() + 1;
    println!("{}",inc_v);
}
```
 
 ### Result
 Result 和 Option 类似，但具有一些额外的优点，即能够存储和错误上下文有关的任意异常值，而不只是 None。当我们希望知道操作失败的原因时，此类型是合适的。
 ```rust
 // 读取文件的Result案例
use std::fs::File;
use std::io::Read;
use std::path::Path;
fn main(){
    let path = Path::new("data.txt");
    let mut file = match  File::open(&path) {
        Ok(file) => file,
        Err(err) => panic!("Error while opening file: {}", err),
    };
    let mut s = String::new();
    let ret = file.read_to_string(&mut s);
    println!("Message: {}",s);
    println!("ret = {:?}", ret);
}
 ```
 
 ### Option/Resul的组合器
 Option/Resul包装器类型附带了很多辅助方法，这被称为**组合器**，其上的实现允许用户轻松地操作内部值。
 组合器之间是可以通过 `ok_or` 或者 `ok` 等类似的方法进行转换的。
 ```rust
 fn get_nth(items: &Vec<usize>, nth: usize) -> Option<usize> {
    if nth < items.len() {
        Some(items[nth])
    }else{
        None
    }
}

fn double(val:usize) -> usize {
    val * val 
}

fn main(){
    let items = vec![7,8,9,1,3,2,4,5,6,67,7];
    println!("{}", items.len());
    let doubled = get_nth(&items, 11).map(double);
    println!("{:?}", doubled);
}
 ```

### 及早返回和运算符?
它的运行机制如下：当我们获得一个成功值时，希望立即提取它；当我们获得一个错误值时，希望提前返回，并将错误传播给调用方。
```rust
use std::string::FromUtf8Error;

fn str_upper_match(str: Vec<u8>) -> Result<String, FromUtf8Error>{
    let ret = match String::from_utf8(str) {
        Ok(str) => str.to_uppercase(),
        Err(err) => return Err(err) // 这里的return 会直接返回
    };
    println!("Conversion succeeded: {}", ret);
    Ok(ret)
}

fn str_upper_concise(str: Vec<u8>) -> Result<String, FromUtf8Error> {
    // 下面使用 运算符 ？ 和 闭包简写
    let ret = String::from_utf8(str).map(|s| s.to_uppercase()) ? ;
    println!("Conversion succeeded:{}", ret);
    Ok(ret)
}

fn main(){
    let invalid_str = str_upper_match(vec![0xe4,0xbd, 0xa0,0xe5,0xa5,0xbd]);
    println!("{:?}", invalid_str);
    let invalid_str = str_upper_concise(vec![0xe4,0xbd, 0xa0,0xe5,0xa5,0xbd]);
    println!("{:?}", invalid_str);
}
```
?运算符还能变成链式调用，只要其中一个出错或者失败了，那么就整体失败，直接返回。
```rust
let _ = File::create("Foo.txt")?.write_all(b"He")?;
```
main函数允许用户返回 Result类型
```rust
fn main() -> Result<(), &'static str> {
    let s = vec!["apple", "mango", "banana"];
    let fourth = s.get(4).ok_or("自定义的错误信息")?;
    println!("fourth {}", fourth);
    Ok(())
}
```

### 不可恢复异常
panic故障机制：它会终止调用它的线程，并返回代码101。如果是子线程被终止，不会影响到其他线程。
默认情况下不会展示堆栈信息，如果需要展示就配置环境变量并运行：`RUST_BACKTRACE=1 cargo run`。
可以使用 `std::panic::catch_unwinid` 函数去更精细的控制故障后的堆栈展开，能够捕获和处理异常，使程序可以从panic中恢复，但不要作为通用的错误处理方法。
```rust
use core::panic;
use std::thread;

fn allice() -> thread::JoinHandle<()> {
    thread::spawn(move || {
        bob();
    })
}

fn bob(){
    malice();
}

fn malice(){
    panic!("malice is panicking!");
}

fn main(){
    let child = allice();
    let _ = child.join();
    bob();
    println!("This is unreachable code");
}
```
```rust
use std::panic;
fn main(){
    panic::catch_unwind(|| {
        panic!("Panicking!");
    }).ok();
    println!("Survived that panic.");
}
```

### 自定义错误和Error特征
自定义的错误特征，必须实现 `Debug` 和 `Display` 这两个特征。也必须实现 `description` 和 `cause` 方法。
```rust
// ! 错误处理
use std::error::Error;
use std::fmt;
use std::fmt::Display;

#[derive(Debug)]
pub enum ParseErr {
    Malformed,
    Empty
}

#[derive(Debug)]
pub struct ReadErr {
    pub child_err: Box<dyn Error>
}

// Error特征要求必须实现的特征
impl Display for ReadErr {
    fn fmt(&self, f: &mut fmt::Formatter ) -> fmt::Result {
        write!(f, "Failed reading todo file")
    }
}
// Error特征要求必须实现的特征
impl Display for ParseErr {
    fn fmt(&self, f: &mut fmt::Formatter ) -> fmt::Result {
        write!(f, "Todo list parsing failed")
    }
}

impl Error for ReadErr {
    fn description(&self) -> &str {
        "Todolist read failed: "
    }
    fn cause(&self) -> Option<&dyn Error> {
        Some(&*self.child_err)
    }
}

impl Error for ParseErr {
    fn description(&self) -> &str {
        "Todolist parsing failed: "
    }
    fn cause(&self) -> Option<&dyn Error> {
        None
    }
} 
//! 该程序库提供了一个解析 todos 列表的 API
use std::fs::read_to_string;
use std::path::Path;

mod error;
use error::ParseErr;
use error::ReadErr;

use std::error::Error;

// 该结构体包含一个解析为 Vec<String>的 todos 列表
#[derive(Debug)]
pub struct TodoList {
    tasks: Vec<String>,
}

impl TodoList {
    pub fn get_todos<P>(path: P) -> Result<TodoList, Box<dyn Error>> 
    where 
        P: AsRef<Path>, 
    {
        let read_todos: Result<String, Box<dyn Error>> = read_todos(path);
        let parsed_todos = parse_todos(&read_todos?)?;
        Ok(parsed_todos)
    }
}

pub fn read_todos<P>(path: P) -> Result<String, Box<dyn Error>>
where 
    P: AsRef<Path>,
{
    let raw_todos = read_to_string(path)
        .map_err(|e| ReadErr {
            child_err: Box::new(e),
        })?;
    Ok(raw_todos)
}

pub fn parse_todos(todo_str: &str) -> Result<TodoList, Box<dyn Error>> {
    let mut tasks: Vec<String> = vec![];
    for i in todo_str.lines() {
        tasks.push(i.to_string());
    }
    if tasks.is_empty() {
        Err(ParseErr::Empty.into())
    }else {
        Ok(TodoList { tasks })
    }
}
```





