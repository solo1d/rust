rust中的String类型是一种比较复杂并且难以处理的类型。
**Rust对在堆上和在堆栈上分配值会区别对待。**
Rust有两种字符串：

1. 包含所有权的字符串（String） 是标准库内容
2. 借用字符串（&str）是编译器能够识别的内置类型，不属于标准库



# 包含所有权的字符串String

String 类型来自标准库，并且是通过堆分配的 UTF-8 编码的字节序列。在底层它们只是 Vec<u8>类型，不过额外包含只适用于字符串的方法。它们是包含所有权的类型，这意味着保存 String 值的变量是其所有者。
```rust
// 创建String类型的几种方法
// 下面四种均创建了相同的字符串类型， 并且具有相同的性能特征。
fn main(){
    // "Hello" 本身是 &str 借用字符串类型
    let a: String = "Hello".to_string(); // to_string来自 ToString特征
    let b = String::from("Hello");	// 是String上的一个关联方法
    let c = "World".to_owned();	// 是ToOwned特征的 to_owned方法创建的
    let d = c.clone();	// 复制已有字符串， 效率最低，会迭代底层字节进行复制
}
```

```rust
// String常用方法
String::new()	// 分配一个空的String类型
String::from(s:&str)	// 分配一个新的String类型,并通过字符串切片来填充
String::with_capacity(capacity: usize)	// 分配一个预定义大小的、空的String
String::from_utf8(vec::Vec<u8>)	// 尝试从bytestring 分配一个新的String类型，参数的内容必须是UTF-8
String的len()方法会提供String类型的长度，兼容Unicode格式。 比如 '中文' 这两个字符在内存中占用4字节，但是len()方法依旧会返回2 
push(ch:char)和push_str(string:&str) 方法用于将字符和字符串切片添加到字符串中
```

```rust
// 调用范例
fn main(){
    let mut empty_string = String::new();
    let empty_string_with_capacity = String::with_capacity(50);
    let string_from_bytestring: String = 
        String::from_utf8(vec![82,85,83,84])
            .expect("Creating String from bytestring failed");
    println!("length of the empty string is {}", empty_string.len());
    println!("length of the empty string with capacity is {}", empty_string_with_capacity.len());
    println!("length of the empty string from a bytestring is {}", string_from_bytestring.len());
    println!("bytestring says {}", string_from_bytestring.len());

    empty_string.push('1');
    println!("1) Empty string now contains {}", empty_string);
    empty_string.push_str("2345");
    println!("2) Empty string now contains {}", empty_string);
    println!("Length of the previously empty string is now {}",empty_string.len());
}
```



# 借用字符串&str

可以将字符串用作被称为字符串切片的引用。

字符串切片默认创建为&str类型的借用字符串，一个指向UTF-8编码字节序列的指针。

我们无法创建和使用裸 str 类型的值，因为它表示有限但大小未知的 UTF-8 编码的连续字节序列。这在技术上被称为不定长类型。

str 只能创建为引用类型。

str 基本意味着一个固定大小的字符串序列，这与其所在的位置无关。它既可以是一个堆分配的字符串引用，也可以是驻留在程序存续期内进程数据段上的 `&'static str` 字符串，这就是'static 生命周期所修饰的内容。

str 表示不定长类型。任何其他 `&str` 类型都是在堆上借用任何拥有String 类型的字符串切片。&str 类型一旦创建就无法更改，因为默认情况下它是不可变的。

`&str` 类型是将自身传递给函数或其他变量时推荐使用的类型。

```rust
fn get_str_literal() -> &'static str {
    "from function"
}

fn main(){
    let my_str = "This is borrowed";
    let from_func = get_str_literal();
    println!("{} {} ", my_str, from_func);
}
```

## 字符串切片和分块

字符串是有效的 UTF-8 编码字节序列，单个字节并不等同于单个字符。在 UTF-8 中，单个字符也可以由多个字节表示。因此索引不适用于字符串。
与所有索引操作的情况类似，如果开始或结束的索引位置不在有效的字符区间，那么会发生灾难性故障。

对字符串的所有字符进行迭代访问的另一种方法是使用 chars 方法，该方法将字符串转换为字符上的迭代器。chars 方法会以适当的 Unicode 边界返回字符串中的字符。

如果需要将一个字符串传递给你的函数，那么请使用字符串切片&str

```rust
fn main(){
    let my_str = String::from("Strings are cool");
    let first_three = &my_str[0..3];  // 切片可以访问，但是单独的 [3] 是不行的
    println!("{:?}", first_three);
}
```



```rust
fn main(){
    let hello = String::from("Hello");
    for c in hello.chars(){		// 迭代器方法
        println!("{}", c);
    }
}
```



```rust
// 字符串切片传递参数
fn say_hello(to_whom: &str){
    println!("Hey {}", to_whom);
}
fn main(){
    let string_slice: &'static str = "you";
    let string: String = string_slice.into();
    say_hello(string_slice);
    say_hello(&string);  // String为str类型实现了类型强制性特征 Deref，该特性确保&String到&str的转换。
}
```



## 字符串拼接

String 和&str 之间主要的差异在于，&str 自身能够被编译器识别，而 String 是标准库中的自定义类型。可以在 `Vec<u8>`之上实现自定义的类似 String 抽象。

```rust
fn main(){
    let a = "Foo"; let b = "Bar";
    let c = a.to_string() + b; // 通过式地将第 1 个字符串转换成包含所有权的字符串来实现两个字符串的拼接
}
```

尽可能使用带有&str 类型的 API，因为当字符串已经分配到某处时，只需引用该字符串就可以节省复制和分配的成本。在程序中传递&str 几乎是零成本的：它几乎不会产生分配成本，也不会复制内存。













