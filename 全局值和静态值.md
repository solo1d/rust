全局值一般有两种：常量和静态值。

常量函数可以调用它们来初始化这些全局值。

全局值只能在初始化时声明非动态的类型，并且在编译期，它在堆栈上的大小是已知的

## 常量

```rust
// 常量全局值
// const 关键字，创建时必须指定类型。  b"" 表示字节文本，用ascii来代替数值
const HEADER: &'static [u8; 4] = b"Obj\0";
fn main(){
    println!("{:?}", HEADER);
    // 输出为： [79, 98, 106, 0]
}
```

## 静态值

静态值是相应的全局值，它们具有固定的内存位置，并且在整个程序中作为单个（唯一）实例存在。

读取和写入静态值都必须在某个 unsafe 代码块中完成。

静态值通常与同步原语搭配使用，它们还用于实现全局锁定，以及与 C 程序库集成。

```rust
static mut BAZ:u32 = 4;
static FOO: u8 = 9;
fn main(){
    unsafe {
        // 必须先读取到局部变量
        let baz = BAZ;
        println!("baz is {}", baz);
        
        BAZ = 42;
        let baz = BAZ; // 重新读取
        println!("baz is now {} ", baz);
        println!("foo is {}", FOO);
    }
}
```



##　编译期函数

编译期计算其参数的常量函数（const 函数）。

const 函数是纯函数，必须是可重现的。这意味着它们不能将可变参数带入任何类型，也不能包含动态的操作，例如堆分配。

```rust
const fn salt(a:u32) -> u32 {
    0xDEADBEEF ^ a
}
const CHECKSUM: u32 = salt(23);
fn main(){
    println!("{}",CHECKSUM);
}
```

```rust
const fn read_header(a:&[u8]) -> (u8, u8, u8, u8) {
    (a[0], a[1], a[2], a[3])
}

// 编译期间执行的内容， 如果文件不存在，那么就直接报错，会将错误在编译时就暴露出来
const FILE_HEADER: (u8, u8, u8, u8) = 
    read_header(include_bytes!("./main.rs"));

fn main(){
    println!("{:?}", FILE_HEADER);
    // 输出：(99, 111, 110, 115)
}
```



## 通过lazy_static!宏将静态值动态化

lazy_static 是第三方软件包实现的，并不是自带的。它暴露了 lazy_static!宏，可用于初始化任何能够从程序中的任何位置全局访问的动态类型。

使用 lazy_static!宏声明的元素需要实现 Sync 特征。这意味着如果某个静态值可变，么必须使用诸如 Mutex 或 RwLock 这样的多线程类型，而不是 RefCell 。

```toml
在 Cargo.toml 中添加依赖

[dependencies]
lazy_static = "1.4.0"
```

```rust
use lazy_static::lazy_static;   // 引入依赖
use std::sync::Mutex;
use std::thread;

// 初始化 Vec 的代码片段，它能够在多线程环境中被修改
lazy_static! {
    static ref ITEMS: Mutex<Vec<u64>> = {
        let mut v =vec![];
        v.push(9);
        v.push(2);
        v.push(1);
        Mutex::new(v)
    };
}

fn main(){
    // 创建多个线程，每个线程向 Vec 中推入一个数字
    let mut handles = vec![];

    for i in 0..5 {
        let handle = thread::spawn(move || {
            // 获取锁，修改 Vec
            let mut guard = ITEMS.lock().unwrap();
            guard.push(i);
            println!("[线程 {}] 推入 {}, 当前长度: {}", i, i, guard.len());
        });
        handles.push(handle);
    }

    // 等待所有线程完成
    for handle in handles {
        handle.join().unwrap();
    }

    // 最后打印整个 Vec 的内容
    let final_vec = ITEMS.lock().unwrap();
    println!("最终 Vec: {:?}", *final_vec);
    // 预期输出顺序可能为 [9, 2, 1, 0, 1, 2, 3, 4]（实际顺序取决于线程调度）
}
```

