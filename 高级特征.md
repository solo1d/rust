# 高级特征

该高级特征在处理复杂代码库时非常重要。

## sized和?sized

Sized 特征是一种标记性特征，用于表示编译期已知大小的类型。它适用于 Rust 中的大多数类型，除了不定长类型之外。所有类型参数在其定义中都具有 sized 特征的隐式限制。我们可以在特征前面添加运算符“?”指定特征的可选边界。

```rust
use std::borrow::Cow;

// 注意：Cow 的泛型参数 B 可以是 str（动态大小），这得益于标准库中
// 对 B 加上了 ?Sized 约束，所以这里 Cow<'_, str> 是合法的。
fn main() {
    // ==========================================
    // 场景 1：函数返回 Cow，智能选择“借用”还是“拥有”
    // ==========================================
    let user_name: &str = "Alice"; // 静态字符串，硬编码在二进制中

    // 调用函数时传入普通 &str
    let message: Cow<'_, str> = generate_message(user_name, false);
    // 此时 message 内部是 Borrowed(&str)，没有发生任何堆分配！
    println!("[只读] 普通消息: {}", message);

    let admin_message: Cow<'_, str> = generate_message("Bob", true);
    // 因为 is_admin 为 true，内部触发了 format!，生成了新的 String，
    // 此时 message 内部是 Owned(String)。
    println!("[拥有] 管理员消息: {}", admin_message);

    // ==========================================
    // 场景 2：手动操作 Cow —— 演示“写时克隆”的核心机制
    // ==========================================
    let static_greeting: &str = "Hello, world!"; // 静态生命周期 &'static str

    // 注意：这里必须声明 mut，因为后面会调用 to_mut() 修改内容
    let mut cow_greeting: Cow<'_, str> = Cow::Borrowed(static_greeting);

    // --- 第一步：不可变访问（零开销） ---
    // 直接调用 str 的方法，内部只是解引用出 &str，完全无克隆。
    println!("原始长度: {}", cow_greeting.len()); // 输出 13
    println!("是否包含 'world': {}", cow_greeting.contains("world")); // true

    // --- 第二步：第一次可变写入（触发克隆！） ---
    // to_mut() 会返回 &mut String。
    // 此时 cow_greeting 内部是 Borrowed，为了不破坏原始静态数据，
    // Rust 自动将 "Hello, world!" 克隆到堆上生成 String，并切换为 Owned 状态。
    cow_greeting.to_mut().push_str(" 欢迎使用 Rust！");

    // 验证修改成功
    println!("第一次修改后: {}", cow_greeting); 
    // 输出：Hello, world! 欢迎使用 Rust！

    // --- 第三步：第二次可变写入（不再克隆！） ---
    // 现在 cow_greeting 内部已经是 Owned，再次调用 to_mut()
    // 直接返回已有 String 的可变引用，无需再次分配内存。
    cow_greeting.to_mut().push('!');

    println!("第二次修改后: {}", cow_greeting);
    // 输出：Hello, world! 欢迎使用 Rust！!

    // 注意：原始的 static_greeting 仍然是 "Hello, world!"，
    // 全程没有被修改或销毁，Cow 保证了安全性。

    // ==========================================
    // 场景 3：提取所有权（终结 Cow）
    // ==========================================
    // into_owned() 消耗 Cow，无论如何都返回一个实实在在的 String。
    // 如果内部是 Borrowed，这里会克隆；如果已经是 Owned，直接转移所有权。
    let final_owned_string: String = cow_greeting.into_owned();
    println!("最终独立拥有的字符串: {}", final_owned_string);

    // 此时 cow_greeting 已被移动，不再可用。
}

/// 模拟一个业务函数：根据是否为管理员生成欢迎语。
/// 
/// 巧妙之处在于：普通用户返回的都是同一个静态字符串（零分配），
/// 而管理员消息需要拼接名字，才触发堆分配。
/// 调用者无需关心内部是借用还是拥有，统一当成 &str 使用即可。
fn generate_message(name: &str, is_admin: bool) -> Cow<'_, str> {
    if is_admin {
        // 只有这里才真正创建新字符串，返回 Owned
        Cow::Owned(format!("【管理员】欢迎您，{}！", name))
    } else {
        // 普通用户直接返回硬编码的静态字符串，零开销借用
        Cow::Borrowed("欢迎访问我们的服务！")
    }
}
```



## Borrow和AsRef

Rust 中的 **`Borrow`** 和 **`AsRef`** 两个 trait，它们都用于“引用转换”，但设计目标和约束不同。

- `AsRef<T>`：通用廉价引用转换
  - **作用**：提供一种**廉价**（通常是 O(1)）的转换，将 `&self` 转换为 `&T`。
  - **特点**：不要求转换后的引用在哈希、相等性等方面与原值保持一致。
  - **典型实现**：`String` 实现 `AsRef<str>`、`Vec<T>` 实现 `AsRef<[T]>`、`PathBuf` 实现 `AsRef<Path>` 等。
  - **使用场景**：用于泛型函数，接受“可以被转换为某种引用”的参数，比如读取文件路径、字符串处理等。
- `Borrow<T>`：带语义约束的引用借用
  - **作用**：同样是将 `&self` 转换为 `&T`，但**额外要求**：转换后的引用必须与原值在 **`Eq`（相等性）、`Ord`（排序）、`Hash`（哈希）** 等方面**行为完全一致**。
  - **特点**：它强加了一个隐含的“等价性契约”，使得 `Borrow` 适合用于**集合的键查找**（如 `HashMap`、`HashSet`）。
  - **典型实现**：`String` 实现 `Borrow<str>`，`Vec<T>` 实现 `Borrow<[T]>`。
  - **使用场景**：当你用 `HashMap<String, V>` 时，可以用 `&str` 调用 `get`，这是因为 `String` 实现了 `Borrow<str>`，且保证了 `&str` 的哈希值与对应 `String` 的哈希值相同。

```rust
```

