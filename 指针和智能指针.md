## 指针类型

三种指针类型：引用（安全的指针）、原始指针、智能指针

### 原始指针

```rust
fn main() {
    let a = &65;
    let a_raw_ptr = a as *const u32;
    let b = &mut 65;
    let a_raw_ptr = b as *mut u32;
}
```

### 智能指针

智能指针具有与之相关联的额外元数据和代码，会在创建和销毁指针时被调用。
智能指针超出作用域时能够自动释放底层资源是采用它们的主要原因之一。
智能指针的两大特性： Drop 和 Deref 

**Drop是特征，会自动释放相关值超出作用域后占用的资源，类似于析构函数。**
Drop包含一个 drop 方法，当对象超出作用域时就会被调用，该方法以 `&mut self` 作为参数。
使用 drop 释放值是以LIFO的方式进行的。

```rust
struct Character {
    name: String,
}

impl Drop for Character {
    fn drop(&mut self) {
        println!("{} went away", self.name);
    }
}

fn main() {
    let steve = Character { name : "Steve".into() };
}
```

**智能指针通常会实现Deref和DerefMut特征，他允许用户对这些类型能够使用解引用`*`运算符，其中 Deref 提供只读签名， DerefMut提供对地产类型的可变引用类型签名。**

- 智能指针的种类：
  - `Box<T>` 它提供了最简单的堆资源分配方式。Box 类型拥有其中的值，并且可用于保存结构体中的值，或者从函数返回它们。<mark>如果基础类型存在Copy特征，那么Box实例会成为副本，否则会发生移动</mark>。
  - `Rc<T>` 他用于引用计数，每当获取新引用时，计数器会执行递增操作，并在用户释放引用时对计数器执行递减操作，当计数器为0时，该值将被移除。它内部存在两种引用， 强引用`Rc<T>` 和 弱引用`Weak<T>`，只有强引用为0才会释放。
  - `Arc<T>` 原子引用计数，和 Rc<T>差不多，但他具有原子性，能保证多线程的安全性。
  - `Cell<T>` 提供了 Copy 特征的类型的内部可变性，可以获得多个可变引用。但是他不支持多线程。
  - `RefCell<T>` 提供了类型的内部可变性，并且不需要实现Copy特征，它用于运行时的锁定以确保安全性，但是他不支持多线程。

```rust
// Box<T> 智能指针的基础使用
fn box_ref<T>(b:T) -> Box<T>{
    let a = b;
    Box::new(a)
}

struct Foo;
struct Node {
    data: u32,
    next: Option<Box<Node>>  // 单链表，这里表示他是个指针+枚举标记
}

fn main(){
    let boxed_one: Box<Foo> = Box::new(Foo);
    let unboxed_one: Foo = *boxed_one;
    box_ref(unboxed_one);

    // 这里创建一个节点
    let node3 = Box::new(Node { data: 3, next: None });
    // 这里创建一个节点，将node3 移动进去， 这个时候 node3 就失效了
    let node2 = Box::new(Node { data: 2, next: Some(node3) });
    let nodeRoot = Box::new(Node { data: 1, next: Some(node2) });
    // 遍历
    let mut current = Some(&nodeRoot); // 这里要取引用
    while let Some(cur) = current {
        println!("Data: {}", cur.data);
        // 我们需要取其引用，用 as_ref() 将 Option<Box> 转为 Option<&Box>
        current = cur.next.as_ref();
    }
}
```

```rust
// Rc<T> 引用指针的使用
use std::{rc::Rc , rc::Weak, cell::RefCell};

#[derive(Debug)]
struct LinkedList<T>{
    head: Option<Rc<LinkedListNode<T>>>
}

#[derive(Debug)]
struct LinkedListNode<T>{
    next: Option<Rc<LinkedListNode<T>>>,
    prev: RefCell<Option<Weak<LinkedListNode<T>>>>,
    data: T
}

impl<T> LinkedList<T>{
    fn new() -> Self {
        LinkedList {head: None }
    }
    fn append(&mut self, data: T) ->Self {
        let new_node: Rc<LinkedListNode<T>> = Rc::new(LinkedListNode{
            data: data,
            next: self.head.clone(), // Rc的 .clone() 是增加引用计数，不是拷贝
            prev: RefCell::new(None)
        });
        match self.head.clone() {
            Some(node) =>{
                let mut prev: std::cell::RefMut<'_, Option<Weak<LinkedListNode<T>>>> = node.prev.borrow_mut();
                *prev = Some(Rc::downgrade(&new_node));
            },
            None =>{}
        }
        LinkedList {
            head: Some(new_node)
        }
    }
}

fn main(){
  let list_of_nums: LinkedList<i32> = LinkedList::new().append(1).append(2).append(3);
  println!("nums: {:?}", list_of_nums);
}
```

```rust
// Cell<T>
use std::cell::Cell;

#[derive(Debug)]
struct Bag {
    item: Box<u32>
}

fn main(){
    let bag = Cell::new(Bag{item: Box::new(1)});

    let hand1 = &bag;
    let hand2 = &bag;
    hand1.set(Bag{item:Box::new(2)});
    hand2.set(Bag{item:Box::new(3)});
}
```

```rust
// RefCell<T>
use std::cell::RefCell;

#[derive(Debug)]
struct Bag {
    item: Box<u32>
}

fn main(){
    let bag = RefCell::new(Bag{item: Box::new(1)});

    let hand1 = &bag;
    let hand2 = &bag;
    *hand1.borrow_mut() = Bag{item:Box::new(2)}; // 新的可变借用
    *hand2.borrow_mut() = Bag{item:Box::new(3)}; // 新的可变借用
    let borrowed = hand1.borrow(); // 接收一个新的不可变借用
    println!("{:?}", borrowed);
}
```



### Cow智能指针类型

Cow 是一种智能指针类型，提供两种版本的字符串，它表示在写入的基础上复制，写时克隆。

```rust
// 类型签名
pub enum Cow<'a, B> 
where 
    B: 'a + ToOwned + ?Sized 
{
    Borrowed(&'a B), // 借用数据
    Owned(<B as ToOwned>::Owned), // 拥有数据
}
```

首先，Cow 有两个变体。

- Borrowed 表示某种类型 B 的借用版本。这个 B 必须实现 ToOwned 特征。
- 所有权变体，其中包含该类型的所有权版本。

此类型适用需要避免不必要的内存分配的情况。一个真实的例子是名为 serde_json 的 JSON 解析器软件包。

```rust
use std::borrow::Cow;

fn sanitize(input: &str) -> Cow<'_, str> {
    if input.contains("bad") {
        // 只有这里才分配新 String
        Cow::Owned(input.replace("bad", "good"))
    } else {
        // 大多数情况直接借用，零分配
        Cow::Borrowed(input)
    }
}

fn main() {
    let s1 = "hello world"; // 静态，不变
    let result1 = sanitize(s1);
    println!("{}", result1); // 输出：hello world（此时仍是借用）

    let s2 = "bad word";
    let result2 = sanitize(s2);
    // 因为调用了 display，仍为不可变，内部可能还是 Owned，但无额外开销
    println!("{}", result2); // 输出：good word

    // 只有当我们想修改它时，才会发生克隆（如果之前是借用的话）
    let mut owned = result1.into_owned(); // 这里如果 result1 是 Borrowed，会克隆成 String
    owned.push_str("!");
}
```

